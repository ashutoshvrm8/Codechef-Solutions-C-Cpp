#include<stdio.h>
#include<string.h>

int main()
{
    char str[1000000];
    int t;
    long long int l,po1,pos2;
    scanf("%d", &t);
    while(t--)
    {
        l=po1=pos2=0;
        fflush(stdin);
        gets(str);
        l=strlen(str);

    }
    return 0;
}
